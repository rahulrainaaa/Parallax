<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.demo.parallax">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".imageswitcher.MyImageSwitcher" />
        <activity android:name=".adapterviewflipper.MyAdapterViewFlipper" />
        <activity android:name=".stackview.MyStackViewActivity" />
        <activity android:name=".textswitcher.MyTextSwitchwer" />
        <activity android:name=".textureview.MyTextureViewActivity" />
        <activity android:name=".viewanimator.MyViewAnimatorActivity"></activity>
    </application>

</manifest>